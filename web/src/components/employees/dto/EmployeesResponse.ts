type EmployeeResponse = {
    id: string;
    date: string; // Assuming the date is received as a string in YYYY-MM-DD format
    timeWork: string;
    fio: string;
    uchastok: string;
    smena: string;
    rank: string;
    sex: string;
}